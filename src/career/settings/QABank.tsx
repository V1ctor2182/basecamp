// Backwards-compat shim — older imports of `./settings/QABank` still resolve.
// The real layout lives at qa-bank/QABankLayout.tsx and is wired in CareerApp.tsx.
export { default } from './qa-bank/QABankLayout'
