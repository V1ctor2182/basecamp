export default function Resumes() {
  return (
    <div className="c-page">
      <h2>Resumes — 多简历管理</h2>
      <p className="c-page-todo">Gallery 视图：多份 base resume（backend / applied-ai / fullstack / default）+ Google Docs 同步 + In-UI 编辑 + Auto-Select 规则。</p>
      <p className="c-page-empty">多简历 Gallery + 编辑 + sync coming in 03-cv-engine/01-resume-index 系列。</p>
      <p className="c-page-ref">Spec: <code>03-cv-engine/01-resume-index</code> + <code>02-google-docs-sync</code> + <code>03-in-ui-editor</code></p>
    </div>
  )
}
