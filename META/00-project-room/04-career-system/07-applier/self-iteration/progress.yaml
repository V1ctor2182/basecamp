milestones: []
current_sprint: "Sprint 20 — Code Calibration shipped"
blockers: []
completion: 100  # 🎉 SUB-EPIC COMPLETE 2026-05-18 (3/3 子 Rooms ✅)
children_completion:
  "01-code-calibration": 100  # 🎉 ROOM COMPLETE 2026-05-18 (4/4 m; fixture corpus + eval runner + auto-tuner + CI smoke)
  "02-data-flywheel": 100     # 🎉 ROOM COMPLETE 2026-05-18 (4/4 m; 4 JSONL stores + Haiku induction + approve/reject seam + Learning tab UI)
  "03-iteration-dashboard": 100  # 🎉 ROOM COMPLETE 2026-05-18 (3/3 m; event aggregator + 5 endpoints + Iteration.tsx + Promote modal + Coverage)
status_note: "🟢 self-iteration SUB-EPIC COMPLETE 2026-05-18 (3/3 子 Rooms ✅). 03-iteration-dashboard ROOM COMPLETE 2026-05-18 (3/3 milestones, 31 asserts smoke green: 23 endpoints + 3 build + 5 promote). m1 event aggregator over 7 existing JSONL stores + 5 REST endpoints + promote-evidence flow (writes stub to data/career/eval-fixtures/promote-queue/, gitignored), m2 Iteration.tsx + Health header + paginated event stream (composite ts+id cursor) + Pending Actions queue + 30s polling, m3 Promote modal (D3 review-truth-yml gate before POST) + Coverage detail collapsible (fixture list + tuner status) + ROOM COMPLETE. Plan-agent + adversarial review across 3 milestones: 0 CRITICAL + 4 HIGH + 4 MEDIUM applied with REVIEW-named regression tests (concurrent-promote race → per-id mutex, pagination boundary skip → composite cursor, EH4 enforcement, poll-vs-load-more race, fetchMore unmount abort, EventKind exhaustiveness). 3 spec overrides locked at plan-milestones (real-time aggregate vs new events.jsonl, promote writes stub only, Tier 2/3 placeholder). 01-code-calibration ROOM COMPLETE 2026-05-18 (4/4 milestones, 131/131 smoke green: 45 fixtures + 37 runner + 33 tuner + 16 CI smoke). m1 fixture schema/loader + capture CLI + 3 seed fixtures, m2 eval runner + 3-dim score + report + CLI, m3 deterministic auto-tuner + ≤5% per-fixture regression gate (EH2) + reviewable diff (EH5), m4 npm scripts + CI smoke + README. Plan-agent + adversarial review across 4 milestones: ~7 CRITICAL + ~16 HIGH + ~13 MEDIUM applied with REVIEW-named regression tests (file:// SSRF, EH4 setContent route-abort, EH2 NaN bypass, --out path traversal, normalizeName drift, triedKeys both-track, candidate cap, etc.). End-to-end: 3 seed fixtures → baseline aggregate min=0% (link role surfaces all footer nav) → tuner proposes `remove link` → min=57% in 1 iteration, converged, no regressions. Next: 03-iteration-dashboard (extends Learning tab with code-fix escalation queue)."
last_updated: "2026-05-18"
